// Single source of truth for site-wide metadata and absolute URLs.
export const siteUrl = "https://preferred.ai";
export const siteName = "Preferred.AI";
export const siteDescription =
  "Preferences and Recommendations from Data & AI";

// Default social-share image (lives in /public). Used when a page has no image of its own.
export const defaultOgImage = "/Brand.png";

// External profiles for structured data (schema.org `sameAs`).
export const siteSameAs = [
  "https://github.com/PreferredAI",
  "https://www.hadylauw.com",
];
